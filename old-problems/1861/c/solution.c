char** rotateTheBox(
    char** boxGrid, int boxGridSize, int* boxGridColSize,
    int* returnSize, int** returnColumnSizes) {
  *returnSize = boxGridSize;
  *returnColumnSizes = boxGridColSize;
  return boxGrid;
}
