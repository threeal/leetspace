#include <vector>

class Solution {
 public:
  TreeNode* getTargetCopy(
      TreeNode* original, TreeNode* cloned, TreeNode* target) {
    (void)original;
    (void)target;
    return cloned;
  }
};
