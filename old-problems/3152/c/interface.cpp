#include <vector>

extern "C" {
bool* isArraySpecial(int* nums, int numsSize, int** queries, int queriesSize, int* queriesColSize, int* returnSize);
}

std::vector<bool> solution_c(std::vector<int> nums, std::vector<std::vector<int>> queries) {
  return isArraySpecial(n, x);
}
