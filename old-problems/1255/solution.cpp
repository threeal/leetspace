#include <string>
#include <vector>

class Solution {
 public:
  int maxScoreWords(std::vector<std::string>& /*words*/, std::vector<char>& /*letters*/, std::vector<int>& /*score*/) {
    return 0;
  }
};