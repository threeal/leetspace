int* xorQueries(
    int* arr, int arrSize,
    int** queries, int queriesSize, int* queriesColSize, int* returnSize) {
  (void)queries;
  (void)queriesSize;
  (void)queriesColSize;
  *returnSize = arrSize;
  return arr;
}
