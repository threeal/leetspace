int* occurrencesOfElement(
    int* nums, int numsSize, int* queries, int queriesSize, int x,
    int* returnSize) {
  (void)queries;
  (void)queriesSize;
  (void)x;
  *returnSize = numsSize;
  return nums;
}
