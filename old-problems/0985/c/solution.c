int* sumEvenAfterQueries(
    int* nums, int numsSize,
    int** queries, int queriesSize, int* queriesColSize,
    int* returnSize) {
  (void)queries;
  (void)queriesSize;
  (void)queriesColSize;
  *returnSize = numsSize;
  return nums;
}
