#include <stdbool.h>

bool* checkIfPrerequisite(
    int numCourses,
    int** prerequisites, int prerequisitesSize, int* prerequisitesColSize,
    int** queries, int queriesSize, int* queriesColSize, int* returnSize) {
  (void)numCourses;
  (void)prerequisites;
  (void)prerequisitesSize;
  (void)prerequisitesColSize;
  (void)queries;
  (void)queriesSize;
  (void)queriesColSize;
  *returnSize = 0;
  return 0;
}
