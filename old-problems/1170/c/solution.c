#include <stdlib.h>

int* numSmallerByFrequency(char** queries, int queriesSize, char** words, int wordsSize, int* returnSize) {
  (void)queries;
  (void)queriesSize;
  (void)words;
  (void)wordsSize;
  *returnSize = 0;
  return NULL;
}
